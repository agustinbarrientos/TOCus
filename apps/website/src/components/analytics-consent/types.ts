import type { WebsiteLocalization } from '../../localization';

/**
 * Current-language labels and Privacy Policy destination.
 * @since 1.0.0
 */
export interface AnalyticsConsentProps {
	localization: Readonly<WebsiteLocalization>;
}
