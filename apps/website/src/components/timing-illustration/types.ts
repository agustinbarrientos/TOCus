import type { WebsiteCatalog } from '../../localization/types';

/**
 * Translated labels for an illustrative pause and browsing window.
 * @since 0.1.0
 */
export interface TimingIllustrationProps {
	catalog: Readonly<WebsiteCatalog>;
}
