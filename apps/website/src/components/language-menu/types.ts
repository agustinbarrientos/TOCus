import type { LocalizedHomePageProperties } from '../../localization';

/**
 * Current locale and real navigation destinations for the header menu.
 * @since 1.0.0
 */
export type LanguageMenuProps = LocalizedHomePageProperties;
