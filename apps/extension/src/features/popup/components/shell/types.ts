/**
 * Localizable messages rendered by the extension popup shell.
 * @since 0.1.0 Initial implementation.
 */
export interface PopupShellCopy {
	/** Short product-stage label. */
	status: string;
	/** Concise explanation of the product purpose. */
	summary: string;
	/** Supporting local-privacy note. */
	foundationNote: string;
}
